# config.properties file stores urls for the test pages, browser information, remote machine information etc.

# Application access details

testSiteURL=https://www.amazon.in/
env=Production
version=TestRun

# Test Browser
test_browser=Firefox
#test_browser=Chrome

# Remote Execution : Set "true" if executing in remote machine through Selenium Driver Server, else set it to "false"
#is_remote=true
is_remote=false

# Remote ip address and credentials
remote_ip=localhost
domain=